# Managed by router-ip-push-hardening.
# Matches only the controlled final Nginx route=fake_N marker.

[Definition]
failregex = ^\s*src=<HOST>\s+route=fake_[0-9]+(?:\s|$)
ignoreregex =
datepattern = {^LN-BEG}%%ExY(?P<_sep>[-/.])%%m(?P=_sep)%%d[T ]%%H:%%M:%%S(?:[.,]%%f)?(?:\s*%%z)?
              {^LN-BEG}
