# Managed by router-ip-push-hardening.
# Matches only the controlled final Nginx route=reject marker.

[Definition]
failregex = ^\s*src=<HOST>\s+route=reject(?:\s|$)
ignoreregex =
datepattern = {^LN-BEG}%%ExY(?P<_sep>[-/.])%%m(?P=_sep)%%d[T ]%%H:%%M:%%S(?:[.,]%%f)?(?:\s*%%z)?
